
block (div, p, h1, ul...) → 한 줄 독차지 → 세로로 쌓임
inline 계열 (input, button...) → 자기 폭만 차지 → 가로로 나란히dma
--------------------------------------------------html
class 는 띄어쓰기 금지, 하이픈으로 연결
class 는 중복가능
id는 고유
--------------------------------------------------css
<script src="script.js"></script>는 body의 제일 아래
카드나 버튼이 미리 존재해야 하기 때문 
"JS 변수엔 const 붙인다" 상수라는 뜻 안 바꾼다 
 DOM(Document Object Model) = JS가 바라보는 "웹페이지


 일반 요소(p, h2, 버튼)의 글자 → .textContent
입력창(input)의 값 → .value

=== (엄격한 비교) — 값도 같고 타입도 같아야 참
== (느슨한 비교) — 비교 전에 타입을 멋대로 바꿔서 맞춰본 뒤 비교